# Polymarket current state

The manually maintained snapshot formerly stored here was retired on
2026-07-14. It had drifted from the VPS and could incorrectly direct operators
back to local Windows processes.

Current state is generated by WO-68 from runtime evidence. Read it on the VPS:

```text
/home/opc/Claude/outputs/performance/operating_state.md
/home/opc/Claude/outputs/performance/operating_state.json
```

The dashboard presents the same state at:

```text
PM_DASHBOARD_PUBLIC_URL from /home/opc/Claude/.env
```

This must resolve to the authenticated tailnet-only URL
`https://<node>.<tailnet>.ts.net/`. The reporting backend on port `8765` is
VPS-loopback-only; a public-IP HTTP URL is not a supported access path.

`docs/OPERATING_STATE.md` defines the row semantics, freshness rules, and
fail-closed behavior. `AGENTS.md` defines the VPS-only operating contract.

Stable constraints remain:

- no Polymarket or SuperBru runtime process runs on the local workstation;
- stale or missing evidence is `UNKNOWN` and cannot authorize activity;
- the system remains shadow/dry-run/paper-gated;
- WO-67 remains blocked until every registered P1-P5 precondition passes;
- promotion-oriented research is restricted to the three hypotheses in
  `docs/EXPERIMENT_REGISTRY.md`;
- crypto up/down remains diagnostic only; and
- no stake, gate, or live-order control may be loosened to force activity.

Repository documentation was reconciled through merged PR #354 on 2026-07-21.
That is a source-history statement only. It does not replace the generated
runtime report, deployment marker, freshness checks, or dashboard evidence.
Open implementation findings are recorded in
`docs/REPOSITORY_LINE_AUDIT_2026-07-21.md`; this page does not mark them fixed.
