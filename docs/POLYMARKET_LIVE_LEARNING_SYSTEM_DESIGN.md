# Continuous-learning design: historical context

The June 2026 local “live learning” design is superseded. It must not be used as
a launcher, architecture map or statement of current health.

The durable ideas remain: point-in-time inputs, chronological/out-of-sample
validation, distinct evidence classes, fail-closed missing data and promotion
only through registered prospective gates. Current promotion-oriented research
is limited to the three hypotheses in `docs/EXPERIMENT_REGISTRY.md`.

All runtime and verification are VPS-only. Current topology is in
`docs/SYSTEM_MAP.md`, dynamic state is generated under
`outputs/performance/operating_state.{md,json}`, and unresolved implementation
risks are in `docs/REPOSITORY_LINE_AUDIT_2026-07-21.md`.

Funding is CLOSED and WO-67 is BLOCKED. “Live” in the historical filename does
not authorize real-money execution.
